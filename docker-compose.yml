version: '3.8'

services:
  mysql:
    image: mysql:5.7
    restart: always
    env_file:
      - .env
    volumes:
      - mysql-data:/var/lib/mysql

  flask-app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mysql

volumes:
  mysql-data:
